<!DOCTYPE html>

<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Turks & Caicos Countdown</title>
<link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=Jost:wght@300;400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --ocean-deep:  #004d50;
    --ocean-mid:   #007f6e;
    --ocean-light: #00d4a0;
    --foam:        #c8f0e8;
    --sand:        #f7e9cc;
    --sun:         #f6c24a;
    --white:       #f0fff8;
  }

- { margin: 0; padding: 0; box-sizing: border-box; }

body {
min-height: 100vh;
background: var(–ocean-deep);
font-family: ‘Jost’, sans-serif;
color: var(–white);
display: flex;
flex-direction: column;
align-items: center;
justify-content: center;
overflow: hidden;
position: relative;
}

/* ── Sky gradient ── */
.sky {
position: absolute;
inset: 0;
background: linear-gradient(
to bottom,
#a8dff5 0%,
#6ec9ef 28%,
#3faede 55%,
#1a8bbf 75%,
#0e6690 100%
);
z-index: 0;
}

/* ── Animated sun ── */
.sun-orb {
position: absolute;
top: -30px;
left: 50%;
transform: translateX(-50%);
width: 140px;
height: 140px;
border-radius: 50%;
background: radial-gradient(circle, #fff9d0 0%, var(–sun) 45%, transparent 75%);
filter: blur(2px);
animation: sunPulse 5s ease-in-out infinite;
z-index: 1;
}

.sun-glow {
position: absolute;
top: -80px;
left: 50%;
transform: translateX(-50%);
width: 280px;
height: 280px;
border-radius: 50%;
background: radial-gradient(circle, rgba(246,194,74,0.25) 0%, transparent 70%);
animation: sunPulse 5s ease-in-out infinite;
z-index: 1;
}

@keyframes sunPulse {
0%, 100% { opacity: 1; transform: translateX(-50%) scale(1); }
50%       { opacity: 0.85; transform: translateX(-50%) scale(1.06); }
}

/* ── Palm silhouette left ── */
.palm-left, .palm-right {
position: absolute;
bottom: 100px;
z-index: 2;
pointer-events: none;
}
.palm-left  { left: -10px; }
.palm-right { right: -10px; transform: scaleX(-1); }

/* ── Layered waves ── */
.ocean {
position: absolute;
bottom: 0; left: 0;
width: 100%; height: 52%;
z-index: 2;
overflow: visible;
}

/* Horizon haze — wide, very blurred band blending sky into sea */
.horizon-haze {
position: absolute;
bottom: 0; left: 0;
width: 100%; height: 52%;
z-index: 3;
pointer-events: none;
/* Sits just above ocean, feathers the meeting point softly */
background: linear-gradient(
to bottom,
rgba(100,200,230,0.55) 0%,
rgba(30,160,190,0.30) 18%,
transparent 45%
);
filter: blur(18px);
}

.wave-layer {
position: absolute;
width: 220%;
height: 100%;
}

.w1 {
/* Rich tropical green-turquoise — clearly distinct from sky blue */
background: linear-gradient(to bottom, #00b894 0%, #007f6e 40%, #004d50 100%);
bottom: 0; opacity: 1;
animation: waveMove 9s linear infinite;
/* Fade top edge so it dissolves into the haze rather than cutting hard */
-webkit-mask-image: linear-gradient(to bottom, transparent 0%, black 22%);
mask-image: linear-gradient(to bottom, transparent 0%, black 22%);
}
.w2 {
background: linear-gradient(to bottom, rgba(0,210,160,0.55) 0%, transparent 60%);
bottom: 10px; opacity: 0.5;
animation: waveMove 12s linear infinite reverse;
animation-delay: -4s;
-webkit-mask-image: linear-gradient(to bottom, transparent 0%, black 25%);
mask-image: linear-gradient(to bottom, transparent 0%, black 25%);
}
.w3 {
background: rgba(0,230,180,0.15);
bottom: 22px;
animation: waveMove 7s linear infinite;
animation-delay: -2s;
-webkit-mask-image: linear-gradient(to bottom, transparent 0%, black 28%);
mask-image: linear-gradient(to bottom, transparent 0%, black 28%);
}

@keyframes waveMove {
0%   { transform: translateX(-50%); }
100% { transform: translateX(0%); }
}

/* ── Sand bar ── */
.sandbar {
position: absolute;
bottom: 0; left: 0;
width: 100%; height: 80px;
background: linear-gradient(to top, #c9954a 0%, #e0b87a 55%, transparent 100%);
z-index: 3;
}

/* ── Foam dots ── */
.foam-dots {
position: absolute;
bottom: 62px; left: 0;
width: 100%; height: 12px;
z-index: 4;
overflow: hidden;
}
.foam-dot {
position: absolute;
background: rgba(255,255,255,0.7);
border-radius: 50%;
animation: foamFade 3s ease-in-out infinite;
}

@keyframes foamFade {
0%, 100% { opacity: 0; }
40%, 60%  { opacity: 1; }
}

/* ── Main card ── */
.card {
position: relative;
z-index: 10;
text-align: center;
padding: 2rem 1.5rem 2.5rem;
max-width: 500px;
width: 92vw;
animation: riseIn 1.2s cubic-bezier(.16,1,.3,1) both;
}

@keyframes riseIn {
from { opacity: 0; transform: translateY(40px); }
to   { opacity: 1; transform: translateY(0); }
}

.eyebrow {
font-size: clamp(0.65rem, 2vw, 0.78rem);
letter-spacing: 0.38em;
text-transform: uppercase;
color: var(–sun);
margin-bottom: 0.7rem;
animation: riseIn 1.2s 0.15s both;
}

h1 {
font-family: ‘DM Serif Display’, serif;
font-size: clamp(2.6rem, 10vw, 5.5rem);
font-style: italic;
line-height: 1.0;
color: var(–white);
text-shadow: 0 2px 24px rgba(2,61,84,0.5);
animation: riseIn 1.2s 0.25s both;
}

.dest-flag {
font-size: clamp(0.8rem, 2.5vw, 0.95rem);
letter-spacing: 0.18em;
color: var(–foam);
margin-top: 0.4rem;
opacity: 0.85;
animation: riseIn 1.2s 0.35s both;
}

/* ── Countdown blocks ── */
.countdown {
display: flex;
justify-content: center;
align-items: flex-end;
gap: clamp(0.5rem, 3vw, 1.4rem);
margin: 2rem 0 1.5rem;
animation: riseIn 1.2s 0.45s both;
}

.block {
display: flex;
flex-direction: column;
align-items: center;
background: rgba(255,255,255,0.10);
backdrop-filter: blur(10px);
border: 1px solid rgba(255,255,255,0.18);
border-radius: 14px;
padding: clamp(0.6rem, 2vw, 1rem) clamp(0.8rem, 3vw, 1.5rem);
min-width: clamp(58px, 16vw, 90px);
box-shadow: 0 8px 32px rgba(2,61,84,0.3);
transition: transform 0.15s ease;
}

.block.pop {
animation: pop 0.25s ease;
}

@keyframes pop {
0%   { transform: scale(1); }
50%  { transform: scale(1.12); }
100% { transform: scale(1); }
}

.block-val {
font-family: ‘DM Serif Display’, serif;
font-size: clamp(1.9rem, 7vw, 3.6rem);
color: var(–white);
line-height: 1;
min-width: 2ch;
text-align: center;
}

.block-lbl {
font-size: clamp(0.55rem, 1.5vw, 0.68rem);
letter-spacing: 0.28em;
text-transform: uppercase;
color: var(–foam);
opacity: 0.75;
margin-top: 0.35rem;
}

.colon {
font-family: ‘DM Serif Display’, serif;
font-size: clamp(1.6rem, 6vw, 3rem);
color: var(–sun);
opacity: 0.7;
padding-bottom: 1.2rem;
line-height: 1;
}

/* ── Departure info ── */
.depart {
font-size: clamp(0.75rem, 2.2vw, 0.88rem);
color: var(–foam);
opacity: 0.7;
letter-spacing: 0.12em;
margin-bottom: 1.8rem;
animation: riseIn 1.2s 0.55s both;
}

.depart span { color: var(–sun); opacity: 1; font-weight: 500; }

/* ── Progress ── */
.progress-wrap {
animation: riseIn 1.2s 0.65s both;
}

.prog-meta {
display: flex;
justify-content: space-between;
font-size: 0.68rem;
letter-spacing: 0.18em;
text-transform: uppercase;
color: var(–foam);
opacity: 0.6;
margin-bottom: 0.5rem;
}

.prog-track {
height: 3px;
background: rgba(255,255,255,0.12);
border-radius: 99px;
overflow: hidden;
}

.prog-fill {
height: 100%;
background: linear-gradient(to right, var(–ocean-light), var(–sun));
border-radius: 99px;
transition: width 1s ease;
}

/* ── Tagline ── */
.tagline {
margin-top: 1.6rem;
font-size: clamp(0.78rem, 2vw, 0.9rem);
font-style: italic;
color: var(–foam);
opacity: 0.5;
letter-spacing: 0.06em;
animation: riseIn 1.2s 0.75s both;
}
</style>

</head>
<body>

<div class="sky"></div>
<div class="sun-glow"></div>
<div class="sun-orb"></div>

<!-- Palm tree SVGs -->

<div class="palm-left">
  <svg width="120" height="260" viewBox="0 0 120 260" fill="none" xmlns="http://www.w3.org/2000/svg">
    <path d="M55 260 Q52 200 48 160 Q44 120 40 90" stroke="#1a3a1f" stroke-width="8" stroke-linecap="round" fill="none"/>
    <!-- fronds -->
    <path d="M40 90 Q10 60 -10 30" stroke="#1a4a1f" stroke-width="5" stroke-linecap="round" fill="none"/>
    <path d="M40 90 Q30 50 50 20"  stroke="#1e5420" stroke-width="5" stroke-linecap="round" fill="none"/>
    <path d="M40 90 Q60 55 80 40"  stroke="#1a4a1f" stroke-width="5" stroke-linecap="round" fill="none"/>
    <path d="M40 90 Q70 80 100 85" stroke="#1e5420" stroke-width="5" stroke-linecap="round" fill="none"/>
    <path d="M40 90 Q15 90 -5 100" stroke="#1a4a1f" stroke-width="4" stroke-linecap="round" fill="none"/>
    <!-- leaves -->
    <ellipse cx="2" cy="28" rx="14" ry="5" fill="#2d6a30" transform="rotate(-30 2 28)"/>
    <ellipse cx="52" cy="16" rx="14" ry="5" fill="#2d6a30" transform="rotate(-80 52 16)"/>
    <ellipse cx="82" cy="38" rx="14" ry="5" fill="#2d6a30" transform="rotate(10 82 38)"/>
    <ellipse cx="102" cy="84" rx="14" ry="5" fill="#2d6a30" transform="rotate(5 102 84)"/>
    <ellipse cx="-8" cy="98" rx="12" ry="4" fill="#2d6a30" transform="rotate(-10 -8 98)"/>
  </svg>
</div>

<div class="palm-right">
  <svg width="120" height="260" viewBox="0 0 120 260" fill="none" xmlns="http://www.w3.org/2000/svg">
    <path d="M55 260 Q52 200 48 160 Q44 120 40 90" stroke="#1a3a1f" stroke-width="8" stroke-linecap="round" fill="none"/>
    <path d="M40 90 Q10 60 -10 30" stroke="#1a4a1f" stroke-width="5" stroke-linecap="round" fill="none"/>
    <path d="M40 90 Q30 50 50 20"  stroke="#1e5420" stroke-width="5" stroke-linecap="round" fill="none"/>
    <path d="M40 90 Q60 55 80 40"  stroke="#1a4a1f" stroke-width="5" stroke-linecap="round" fill="none"/>
    <path d="M40 90 Q70 80 100 85" stroke="#1e5420" stroke-width="5" stroke-linecap="round" fill="none"/>
    <path d="M40 90 Q15 90 -5 100" stroke="#1a4a1f" stroke-width="4" stroke-linecap="round" fill="none"/>
    <ellipse cx="2" cy="28" rx="14" ry="5" fill="#2d6a30" transform="rotate(-30 2 28)"/>
    <ellipse cx="52" cy="16" rx="14" ry="5" fill="#2d6a30" transform="rotate(-80 52 16)"/>
    <ellipse cx="82" cy="38" rx="14" ry="5" fill="#2d6a30" transform="rotate(10 82 38)"/>
    <ellipse cx="102" cy="84" rx="14" ry="5" fill="#2d6a30" transform="rotate(5 102 84)"/>
    <ellipse cx="-8" cy="98" rx="12" ry="4" fill="#2d6a30" transform="rotate(-10 -8 98)"/>
  </svg>
</div>

<div class="ocean">
  <div class="wave-layer w3"></div>
  <div class="wave-layer w2"></div>
  <div class="wave-layer w1"></div>
</div>
<div class="horizon-haze"></div>

<div class="sandbar"></div>
<div class="foam-dots" id="foamDots"></div>

<div class="card">
  <p class="eyebrow">✦ Next stop ✦</p>
  <h1>Turks &amp; Caicos</h1>
  <p class="dest-flag">🇹🇨 &nbsp;Providenciales &nbsp;·&nbsp; Caribbean</p>

  <div class="countdown">
    <div class="block" id="b-days">
      <span class="block-val" id="days">--</span>
      <span class="block-lbl">Days</span>
    </div>
    <div class="colon">:</div>
    <div class="block" id="b-hrs">
      <span class="block-val" id="hrs">--</span>
      <span class="block-lbl">Hours</span>
    </div>
    <div class="colon">:</div>
    <div class="block" id="b-mins">
      <span class="block-val" id="mins">--</span>
      <span class="block-lbl">Mins</span>
    </div>
    <div class="colon">:</div>
    <div class="block" id="b-secs">
      <span class="block-val" id="secs">--</span>
      <span class="block-lbl">Secs</span>
    </div>
  </div>

  <p class="depart">✈️ &nbsp;Departure: <span>May 16, 2026 · 6:00 AM EST</span></p>

  <div class="progress-wrap">
    <div class="prog-meta">
      <span>Today</span>
      <span id="pct-lbl">0% of the wait done</span>
      <span>Takeoff ✈️</span>
    </div>
    <div class="prog-track">
      <div class="prog-fill" id="fill" style="width:0%"></div>
    </div>
  </div>

  <p class="tagline">Grace Bay awaits — just a little longer ☀️</p>
</div>

<script>
  // ── Foam dots ──
  const foamWrap = document.getElementById('foamDots');
  for (let i = 0; i < 28; i++) {
    const d = document.createElement('div');
    d.className = 'foam-dot';
    const size = 3 + Math.random() * 7;
    d.style.cssText = `
      width:${size}px; height:${size}px;
      left:${Math.random()*100}%;
      top:${Math.random()*10}px;
      animation-delay:${Math.random()*3}s;
      animation-duration:${2+Math.random()*2}s;
    `;
    foamWrap.appendChild(d);
  }

  // ── Countdown target: May 16, 2026 6:00 AM EST = UTC-5 ──
  // EST is UTC-5. May is standard time so EST applies (not EDT which is UTC-4).
  // Actually in May, US eastern time is EDT (UTC-4). Let's use UTC-4.
  // 6:00 AM EDT = 10:00 AM UTC
  const target = new Date(Date.UTC(2026, 4, 16, 10, 0, 0)); // May=4 (0-indexed)

  // For progress bar: reference start = now at page load
  const startMs = Date.now();
  const totalMs = target.getTime() - startMs;

  function pad(n) { return String(Math.floor(n)).padStart(2, '0'); }

  let prev = { d: -1, h: -1, m: -1, s: -1 };

  function triggerPop(blockId) {
    const el = document.getElementById(blockId);
    el.classList.remove('pop');
    void el.offsetWidth;
    el.classList.add('pop');
  }

  function tick() {
    const now = Date.now();
    const diff = target.getTime() - now;

    if (diff <= 0) {
      ['days','hrs','mins','secs'].forEach(id => document.getElementById(id).textContent = '00');
      document.getElementById('pct-lbl').textContent = '🎉 You\'re on vacation!';
      document.getElementById('fill').style.width = '100%';
      return;
    }

    const totalSecs = Math.floor(diff / 1000);
    const d = Math.floor(totalSecs / 86400);
    const h = Math.floor((totalSecs % 86400) / 3600);
    const m = Math.floor((totalSecs % 3600) / 60);
    const s = totalSecs % 60;

    if (d !== prev.d) { document.getElementById('days').textContent = d;     triggerPop('b-days'); prev.d = d; }
    if (h !== prev.h) { document.getElementById('hrs').textContent  = pad(h); triggerPop('b-hrs');  prev.h = h; }
    if (m !== prev.m) { document.getElementById('mins').textContent = pad(m); triggerPop('b-mins'); prev.m = m; }
    if (s !== prev.s) { document.getElementById('secs').textContent = pad(s); triggerPop('b-secs'); prev.s = s; }

    const elapsed = now - startMs;
    const pct = Math.min(100, (elapsed / totalMs) * 100);
    document.getElementById('fill').style.width = pct + '%';
    document.getElementById('pct-lbl').textContent = pct.toFixed(2) + '% of the wait done';
  }

  tick();
  setInterval(tick, 1000);
</script>

</body>
</html>
